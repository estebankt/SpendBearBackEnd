# azure-pipelines.yml
# Replaces .github/workflows/azure-deploy.yml

parameters:
  - name: deployTarget
    displayName: 'Deploy to'
    type: string
    default: 'staging'
    values:
      - staging
      - production
      - none

trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - main

variables:
  DOTNET_VERSION: '10.x'
  BUILD_CONFIGURATION: 'Release'
  PACKAGE_PATH: '$(Build.ArtifactStagingDirectory)/publish'
  STAGING_APP_NAME: 'spendbear-api-dev'
  PRODUCTION_APP_NAME: 'spendbear-api'
  AZURE_SERVICE_CONNECTION: 'azure-spendbear'

stages:

# ─────────────────────────────────────────────
- stage: Build
  displayName: 'Build & Test'
  jobs:
  - job: BuildTest
    displayName: 'Build, Test, Publish'
    pool:
      vmImage: 'ubuntu-latest'

    steps:
    - task: UseDotNet@2
      displayName: 'Install .NET $(DOTNET_VERSION)'
      inputs:
        version: $(DOTNET_VERSION)

    - task: DotNetCoreCLI@2
      displayName: 'Restore'
      inputs:
        command: restore
        projects: '**/*.csproj'

    - task: DotNetCoreCLI@2
      displayName: 'Build'
      inputs:
        command: build
        arguments: '--configuration $(BUILD_CONFIGURATION) --no-restore'

    - script: |
        dotnet test \
          --configuration $(BUILD_CONFIGURATION) \
          --no-build \
          --filter "FullyQualifiedName~Domain|Application" \
          --logger trx \
          --collect:"XPlat Code Coverage" \
          --results-directory $(Agent.TempDirectory)/TestResults
      displayName: 'Unit Tests'

    - task: PublishTestResults@2
      displayName: 'Publish Test Results'
      condition: always()
      inputs:
        testResultsFormat: VSTest
        testResultsFiles: '$(Agent.TempDirectory)/TestResults/**/*.trx'

    - task: DotNetCoreCLI@2
      displayName: 'Publish API'
      inputs:
        command: publish
        publishWebProjects: false
        projects: 'src/Api/SpendBear.Api/SpendBear.Api.csproj'
        arguments: '--configuration $(BUILD_CONFIGURATION) --no-build --output $(PACKAGE_PATH)'
        zipAfterPublish: true

    - task: PublishBuildArtifacts@1
      displayName: 'Upload api-package artifact'
      inputs:
        PathtoPublish: '$(PACKAGE_PATH)'
        ArtifactName: 'api-package'

    - script: |
        dotnet tool install --global dotnet-ef
        echo "##vso[task.setvariable variable=PATH]$PATH:$HOME/.dotnet/tools"
        mkdir -p $(Build.ArtifactStagingDirectory)/migrations
      displayName: 'Install EF Core tools'

    - script: |
        dotnet ef migrations bundle \
          --project src/Modules/Identity/Identity.Infrastructure \
          --startup-project src/Api/SpendBear.Api \
          --context IdentityDbContext \
          --no-build --configuration Release \
          --self-contained -r linux-x64 \
          --output $(Build.ArtifactStagingDirectory)/migrations/efbundle-identity \
          --force
      displayName: 'Bundle: Identity'

    - script: |
        dotnet ef migrations bundle \
          --project src/Modules/Spending/Spending.Infrastructure \
          --startup-project src/Api/SpendBear.Api \
          --context SpendingDbContext \
          --no-build --configuration Release \
          --self-contained -r linux-x64 \
          --output $(Build.ArtifactStagingDirectory)/migrations/efbundle-spending \
          --force
      displayName: 'Bundle: Spending'

    - script: |
        dotnet ef migrations bundle \
          --project src/Modules/Budgets/Budgets.Infrastructure \
          --startup-project src/Api/SpendBear.Api \
          --context BudgetsDbContext \
          --no-build --configuration Release \
          --self-contained -r linux-x64 \
          --output $(Build.ArtifactStagingDirectory)/migrations/efbundle-budgets \
          --force
      displayName: 'Bundle: Budgets'

    - script: |
        dotnet ef migrations bundle \
          --project src/Modules/Notifications/Notifications.Infrastructure \
          --startup-project src/Api/SpendBear.Api \
          --context NotificationsDbContext \
          --no-build --configuration Release \
          --self-contained -r linux-x64 \
          --output $(Build.ArtifactStagingDirectory)/migrations/efbundle-notifications \
          --force
      displayName: 'Bundle: Notifications'

    - script: |
        dotnet ef migrations bundle \
          --project src/Modules/Analytics/Analytics.Infrastructure \
          --startup-project src/Api/SpendBear.Api \
          --context AnalyticsDbContext \
          --no-build --configuration Release \
          --self-contained -r linux-x64 \
          --output $(Build.ArtifactStagingDirectory)/migrations/efbundle-analytics \
          --force
      displayName: 'Bundle: Analytics'

    - script: |
        dotnet ef migrations bundle \
          --project src/Modules/StatementImport/StatementImport.Infrastructure \
          --startup-project src/Api/SpendBear.Api \
          --context StatementImportDbContext \
          --no-build --configuration Release \
          --self-contained -r linux-x64 \
          --output $(Build.ArtifactStagingDirectory)/migrations/efbundle-statementimport \
          --force
      displayName: 'Bundle: StatementImport'

    - script: |
        cp src/Shared/SpendBear.Infrastructure.Core/Outbox/outbox-init.sql \
          $(Build.ArtifactStagingDirectory)/migrations/outbox-init.sql
      displayName: 'Bundle: copy outbox-init.sql'

    - task: PublishPipelineArtifact@1
      displayName: 'Publish migration bundles'
      inputs:
        targetPath: '$(Build.ArtifactStagingDirectory)/migrations'
        artifactName: 'migration-bundles'

# ─────────────────────────────────────────────
- stage: Migrate
  displayName: 'Apply Database Migrations'
  dependsOn: Build
  condition: |
    and(
      succeeded(),
      eq(variables['Build.SourceBranch'], 'refs/heads/main'),
      or(
        eq(variables['Build.Reason'], 'IndividualCI'),
        and(eq(variables['Build.Reason'], 'Manual'), ne('${{ parameters.deployTarget }}', 'none'))
      )
    )
  variables:
  - group: spendbear-production-vars
  jobs:
  - deployment: migrate_staging_db
    displayName: 'Run Migration Bundles (Staging)'
    environment: 'Staging'
    variables:
    - group: spendbear-staging-vars
    pool:
      vmImage: 'ubuntu-latest'
    strategy:
      runOnce:
        deploy:
          steps:
          - download: current
            artifact: migration-bundles

          - script: chmod +x $(Pipeline.Workspace)/migration-bundles/efbundle-*
            displayName: 'Make bundles executable'

          - script: |
              export ConnectionStrings__DefaultConnection="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              $(Pipeline.Workspace)/migration-bundles/efbundle-identity \
                --connection "$(CONNECTIONSTRING_DEFAULTCONNECTION)"
            displayName: 'Migrate: Identity'

          - script: |
              export ConnectionStrings__DefaultConnection="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              $(Pipeline.Workspace)/migration-bundles/efbundle-spending \
                --connection "$(CONNECTIONSTRING_DEFAULTCONNECTION)"
            displayName: 'Migrate: Spending'

          - script: |
              export ConnectionStrings__DefaultConnection="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              $(Pipeline.Workspace)/migration-bundles/efbundle-budgets \
                --connection "$(CONNECTIONSTRING_DEFAULTCONNECTION)"
            displayName: 'Migrate: Budgets'

          - script: |
              export ConnectionStrings__DefaultConnection="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              $(Pipeline.Workspace)/migration-bundles/efbundle-notifications \
                --connection "$(CONNECTIONSTRING_DEFAULTCONNECTION)"
            displayName: 'Migrate: Notifications'

          - script: |
              export ConnectionStrings__DefaultConnection="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              $(Pipeline.Workspace)/migration-bundles/efbundle-analytics \
                --connection "$(CONNECTIONSTRING_DEFAULTCONNECTION)"
            displayName: 'Migrate: Analytics'

          - script: |
              export ConnectionStrings__DefaultConnection="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              $(Pipeline.Workspace)/migration-bundles/efbundle-statementimport \
                --connection "$(CONNECTIONSTRING_DEFAULTCONNECTION)"
            displayName: 'Migrate: StatementImport'

          - script: |
              CONN="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              parse() { echo "$CONN" | tr ';' '\n' | grep -i "^$1=" | head -1 | sed "s/^[^=]*=//"; }
              export PGHOST=$(parse "Host")
              export PGDATABASE=$(parse "Database")
              export PGUSER=$(parse "Username")
              export PGPASSWORD=$(parse "Password")
              export PGPORT=$(parse "Port"); export PGPORT=${PGPORT:-5432}
              SSL=$(parse "SSL Mode"); export PGSSLMODE=${SSL:-require}; export PGSSLMODE=$(echo "$PGSSLMODE" | tr '[:upper:]' '[:lower:]')
              psql -f $(Pipeline.Workspace)/migration-bundles/outbox-init.sql
            displayName: 'Migrate: Shared Outbox Table'

  - deployment: migrate_db
    displayName: 'Run Migration Bundles (Production)'
    environment: 'Production'
    pool:
      vmImage: 'ubuntu-latest'
    strategy:
      runOnce:
        deploy:
          steps:
          - download: current
            artifact: migration-bundles

          - script: chmod +x $(Pipeline.Workspace)/migration-bundles/efbundle-*
            displayName: 'Make bundles executable'

          - script: |
              export ConnectionStrings__DefaultConnection="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              $(Pipeline.Workspace)/migration-bundles/efbundle-identity \
                --connection "$(CONNECTIONSTRING_DEFAULTCONNECTION)"
            displayName: 'Migrate: Identity'

          - script: |
              export ConnectionStrings__DefaultConnection="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              $(Pipeline.Workspace)/migration-bundles/efbundle-spending \
                --connection "$(CONNECTIONSTRING_DEFAULTCONNECTION)"
            displayName: 'Migrate: Spending'

          - script: |
              export ConnectionStrings__DefaultConnection="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              $(Pipeline.Workspace)/migration-bundles/efbundle-budgets \
                --connection "$(CONNECTIONSTRING_DEFAULTCONNECTION)"
            displayName: 'Migrate: Budgets'

          - script: |
              export ConnectionStrings__DefaultConnection="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              $(Pipeline.Workspace)/migration-bundles/efbundle-notifications \
                --connection "$(CONNECTIONSTRING_DEFAULTCONNECTION)"
            displayName: 'Migrate: Notifications'

          - script: |
              export ConnectionStrings__DefaultConnection="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              $(Pipeline.Workspace)/migration-bundles/efbundle-analytics \
                --connection "$(CONNECTIONSTRING_DEFAULTCONNECTION)"
            displayName: 'Migrate: Analytics'

          - script: |
              export ConnectionStrings__DefaultConnection="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              $(Pipeline.Workspace)/migration-bundles/efbundle-statementimport \
                --connection "$(CONNECTIONSTRING_DEFAULTCONNECTION)"
            displayName: 'Migrate: StatementImport'

          - script: |
              CONN="$(CONNECTIONSTRING_DEFAULTCONNECTION)"
              parse() { echo "$CONN" | tr ';' '\n' | grep -i "^$1=" | head -1 | sed "s/^[^=]*=//"; }
              export PGHOST=$(parse "Host")
              export PGDATABASE=$(parse "Database")
              export PGUSER=$(parse "Username")
              export PGPASSWORD=$(parse "Password")
              export PGPORT=$(parse "Port"); export PGPORT=${PGPORT:-5432}
              SSL=$(parse "SSL Mode"); export PGSSLMODE=${SSL:-require}; export PGSSLMODE=$(echo "$PGSSLMODE" | tr '[:upper:]' '[:lower:]')
              psql -f $(Pipeline.Workspace)/migration-bundles/outbox-init.sql
            displayName: 'Migrate: Shared Outbox Table'

# ─────────────────────────────────────────────
- stage: DeployStaging
  displayName: 'Deploy → Staging'
  dependsOn: [Build, Migrate]
  condition: |
    and(
      succeeded(),
      eq(variables['Build.SourceBranch'], 'refs/heads/main'),
      or(
        eq(variables['Build.Reason'], 'IndividualCI'),
        and(eq(variables['Build.Reason'], 'Manual'), eq('${{ parameters.deployTarget }}', 'staging'))
      )
    )
  variables:
  - group: spendbear-staging-vars
  jobs:
  - deployment: DeployStaging
    displayName: 'Deploy to Staging App Service'
    environment: 'Staging'
    pool:
      vmImage: 'ubuntu-latest'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: AzureWebApp@1
            displayName: 'Deploy to spendbear-api-dev'
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              appType: webApp
              appName: $(STAGING_APP_NAME)
              package: '$(Pipeline.Workspace)/api-package/**/*.zip'

          - task: AzureAppServiceSettings@1
            displayName: 'Apply staging app settings'
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              appName: $(STAGING_APP_NAME)
              appSettings: |
                [
                  {"name": "ASPNETCORE_ENVIRONMENT", "value": "Staging", "slotSetting": false},
                  {"name": "Auth0__Domain",   "value": "$(AUTH0_DOMAIN)",   "slotSetting": false},
                  {"name": "Auth0__Audience", "value": "$(AUTH0_AUDIENCE)", "slotSetting": false},
                  {"name": "ConnectionStrings__DefaultConnection", "value": "$(CONNECTIONSTRING_DEFAULTCONNECTION)", "slotSetting": false}
                ]

          - script: |
              echo "Waiting for app to start..."
              sleep 30
              for i in 1 2 3; do
                if curl -sf https://$(STAGING_APP_NAME).azurewebsites.net/health; then
                  echo "✅ Staging smoke test passed"
                  exit 0
                fi
                echo "Attempt $i failed, retrying in 10s..."
                sleep 10
              done
              echo "❌ Health check failed"
              exit 1
            displayName: 'Smoke Test'

# ─────────────────────────────────────────────
- stage: DeployProduction
  displayName: 'Deploy → Production'
  dependsOn: [Build, Migrate, DeployStaging]
  condition: |
    and(
      in(dependencies.Build.result, 'Succeeded'),
      in(dependencies.Migrate.result, 'Succeeded'),
      in(dependencies.DeployStaging.result, 'Succeeded', 'Skipped'),
      eq(variables['Build.Reason'], 'Manual'),
      eq('${{ parameters.deployTarget }}', 'production')
    )
  variables:
  - group: spendbear-production-vars
  jobs:
  - deployment: DeployProduction
    displayName: 'Deploy to Production App Service'
    environment: 'Production'
    pool:
      vmImage: 'ubuntu-latest'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: AzureWebApp@1
            displayName: 'Deploy to spendbear-api'
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              appType: webApp
              appName: $(PRODUCTION_APP_NAME)
              package: '$(Pipeline.Workspace)/api-package/**/*.zip'

          - task: AzureAppServiceSettings@1
            displayName: 'Apply production app settings'
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              appName: $(PRODUCTION_APP_NAME)
              appSettings: |
                [
                  {"name": "Auth0__Domain",   "value": "$(AUTH0_DOMAIN)",   "slotSetting": false},
                  {"name": "Auth0__Audience", "value": "$(AUTH0_AUDIENCE)", "slotSetting": false},
                  {"name": "ConnectionStrings__DefaultConnection", "value": "$(CONNECTIONSTRING_DEFAULTCONNECTION)", "slotSetting": false}
                ]

          - script: |
              sleep 30
              for i in 1 2 3; do
                if curl -sf https://$(PRODUCTION_APP_NAME).azurewebsites.net/health; then
                  echo "✅ Production smoke test passed"
                  exit 0
                fi
                sleep 10
              done
              exit 1
            displayName: 'Smoke Test'
